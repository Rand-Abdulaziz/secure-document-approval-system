services:
  backend:
    build:
      context: ./backend
    container_name: securedoc-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./backend/credentials:/app/credentials